<UserControl x:Class="LocalFileSharing.DesktopUI.Views.ConnectedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocalFileSharing.DesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer
            Grid.Column="1"
            HorizontalScrollBarVisibility="Auto" 
            VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ProgressBar x:Name="Download" Height="50" />
                <Button x:Name="Receive" Content="Receive" />
                <ProgressBar x:Name="Upload" Height="50" />
                <Button x:Name="Send" Content="Send" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
